import { describe, it, expect } from "vitest";

describe("SSR guards", () => {
  it("unauthenticated redirects to /(auth)/sign-in (placeholder)", () => {
    expect(true).toBe(true);
  });
  it("non-citizen signs out and redirects (placeholder)", () => {
    expect(true).toBe(true);
  });
});


