---
alwaysApply: false
---
Use the Supabase CLI outputs locally: API (e.g., http://localhost:54321), Studio (e.g., http://localhost:54323); copy exactly into NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.

Never use Studio URL as API in apps; API uses the gateway URL/port (e.g., 54321).

Keep .env files out of Git; provide .env.example with placeholders; ensure origins match to avoid CORS issues.