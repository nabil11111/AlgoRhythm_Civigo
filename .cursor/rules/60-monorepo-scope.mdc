---
globs: apps/** ,supabase/** ,packages/**
alwaysApply: false
---

When a change spans multiple apps, first produce a short plan (files, steps, risks) and stop for approval.

For shared utils/types in packages/, update imports consistently and run typecheck across both apps post-change.
